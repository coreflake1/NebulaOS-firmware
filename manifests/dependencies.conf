# Authoritative dependency pin manifest for the NebulaOS firmware build.
#
# One rule: every external source this build depends on has its exact pinned
# ref recorded HERE, and nowhere else. scripts/build/00-fetch-vendor-sources.sh
# sources this file rather than hardcoding pins inline (previously each pin
# lived as a shell variable scattered across that script - functionally
# equivalent, but "the one obvious authoritative file" wasn't obvious).
#
# Format: plain shell variable assignments (this file is `.`-sourced by
# 00-fetch-vendor-sources.sh directly - no parsing dependency, no jq/python
# needed this early in the pipeline). Comments and blank lines allowed.
#
# To bump a pin: edit the value below, in its own reviewed commit, then
# re-run 00-fetch-vendor-sources.sh - it fails loudly if the ref doesn't
# resolve, and re-verifies every pin on every run (not just first clone).
#
# A dependency with NO entry here, or an entry left as the placeholder
# UNPINNED_MUST_SET_BEFORE_BUILD, causes 00-fetch-vendor-sources.sh to fail
# immediately, by design - see its own "required pin" check.

# --- Kernel (coreflake1/NebulaOS-kernel, `openke` branch, sparse-checked-out) ---
#
# 2026-08-14 (repository canonicalization mission): renamed from
# coreflake1/NebulaOS to coreflake1/NebulaOS-kernel via GitHub's rename
# mechanism (all branches/tags/commit hashes preserved, old URL still
# redirects) - the old name collided with the intended end-user release
# repo identity. Updated here to the new canonical URL rather than relying
# on the redirect indefinitely. KERNEL_PIN itself is unchanged (295b7101,
# same commit, same content, only the repo's name changed).
KERNEL_REPO=https://github.com/coreflake1/NebulaOS-kernel.git
KERNEL_BRANCH=openke
KERNEL_PIN=295b7101d751fd888ae39e6f1746a4a940664a5f

# --- Buildroot config for this board family ---
BUILDROOT_REPO=https://github.com/lone0/buildroot-x2000.git
BUILDROOT_PIN=74d020081096972857acdb9e76c6c5335455d430

# --- SimpleAF's real workflow/config/installer repo (config fragments only - see
# 00-fetch-vendor-sources.sh's own comment on which files are actually used) ---
PELLCORP_CREALITY_REPO=https://github.com/pellcorp/creality.git
PELLCORP_CREALITY_PIN=d18d354456a89c20507e574feaa34d6389e679ca

# --- Klipper runtime (OFFICIAL, unmodified Klipper3d/klipper - NOT a fork) ---
#
# 2026-08-17 (Phase 1 no-fork migration): KLIPPER_REPO moved from
# coreflake1/NebulaOS-klipper to the official upstream. This is the headline
# change of the whole Phase 1 architecture, and it is not a cosmetic
# re-pointing: NebulaOS no longer hosts a Klipper fork at all. Everything
# this project owns (PRTouch, z-compensation, TMC status, the vendored
# community extras) now lives in KLIPPER_EXTENSIONS_REPO below and is
# activated at boot by symlink composition into this checkout's
# klippy/extras/, listed in that clone's own .git/info/exclude so the
# checkout stays content-pristine. See
# _project/missions/2026-08-phase1-klipper-no-fork-analysis.md for the
# feasibility proof and docs/NEBULAOS_KLIPPER_COMPOSITION.md for how the
# runtime side works.
#
# Core Klipper file patches still required under this architecture: ZERO.
# The three the old fork carried each dissolve for a different reason -
# TRSYNC_TIMEOUT is provably unreachable on a single-MCU printer (mainline
# selects TRSYNC_SINGLE_MCU_TIMEOUT instead), chelper is a firmware
# packaging problem solved by the mtime invariant in
# /etc/nebulaos-chelper-preflight.sh, and the GD32 die-temperature curves
# register through heaters.add_sensor_factory() from an ordinary external
# extra.
#
# A direct consequence worth stating plainly: Moonraker's reserved
# [update_manager klipper] slot hardcodes origin =
# https://github.com/Klipper3d/klipper.git and does not allow it to be
# overridden. The old fork therefore tripped Moonraker's "Unofficial remote
# url" anomaly on every refresh, by construction. Pointing at the official
# remote REMOVES a divergence rather than creating one.
#
# The pin below is the qualification candidate, and it is what
# [update_manager klipper]'s pinned_commit in
# printer_data/config/nebulaos/klipper-pin.conf must match. Moving it is a
# deliberate act with a fixed procedure (compose, run the extension suite,
# re-verify required_klipper_symbols, rebuild c_helper.so, re-qualify on
# hardware, then move both the pin and the manifest together) - see
# NebulaOS-klipper-extensions/docs/COMPATIBILITY.md's "Moving the qualified
# pin".
#
# HISTORICAL - the fork's own pin history is preserved below for
# provenance. coreflake1/NebulaOS-klipper is NOT deleted or archived by
# this change; it remains the historical fork of record, and the extension
# repository carries its real filtered history.
#
# 2026-08-07 baseline-repair mission: bumped from d839d0375a31327e57e0a35e-
# 99e70ba60814ec05 to the commit that reconciles this fork's tracked
# klippy/extras/ with what's actually running on the real printer (the
# z_compensate structured status contract + the load-cell config
# reconciliation fixes, previously local-only/manually-deployed - see
# scripts/build/apply-qualified-baseline.sh's own mission history and
# docs/NEBULAOS_QUALIFIED_BASELINE_VARIANT_AUDIT.md).
#
# Pushed 2026-08-07 (confirmed resolvable from a fresh clone as part of
# this mission's own clean-room reproducibility test).
#
# 2026-08-07 (clean-update mission): KLIPPER_BRANCH switched from
# `nebulaos` to `master` - real bug found live: Moonraker's reserved
# `[update_manager klipper]` slot auto-discovers path/branch from the
# live checkout, and the device's persistent checkout was on `master`
# (the branch Moonraker/factory-seed have always assumed is canonical -
# see S04nebulaos-factory-seed's own doc comment), which was still
# sitting at the OLD pinned commit while every accepted fix landed on a
# separate `nebulaos` side-branch. Fast-forwarded master to the accepted
# HEAD (pure linear fast-forward, verified: master's old tip is a direct
# ancestor of this pin, zero rebase/force-push) rather than repointing
# Moonraker at a second branch - one canonical branch, not two.
# `nebulaos` still exists (unchanged, not deleted/rewritten) as a
# historical record of where this exact commit was first pushed.
#
# 2026-08-08 (Final Baseline Closure mission): bumped from 462fd689 to
# 845396f0, which untracks klippy/chelper/c_helper.so and restores
# upstream Klipper's own `*.so` .gitignore entry (this fork had it
# commented out). Pure linear fast-forward on master (verified: 462fd689
# is a direct ancestor). See docs/NEBULAOS_C_HELPER_DIRTY_STATE_FIX.md
# for the full root-cause writeup (read directly from the pinned
# Moonraker source) of why a tracked, always-different cross-compiled
# binary made Moonraker's own update_manager report Klipper as
# permanently dirty/invalid on every real device.
#
# 2026-08-09 (load-cell safety hardening mission): bumped from 845396f0 to
# 4510ee65, the mirror commit of this repo's own klippy_extras/ changes -
# see docs/NEBULAOS_LOADCELL_SAFETY_HARDENING.md. Real process gap found
# and closed this mission: this repo's own klippy_extras/ directory is
# NOT injected at build time (04-cross-compile-app-stack.sh's own comment,
# confirmed by direct rootfs inspection - a build from a clean klippy_extras/
# edit alone silently shipped the OLD, pre-mission prtouch_probe.py with
# none of this mission's fixes) - vendor/klipper's own klippy/extras/,
# pulled via this exact pin, is the real build input. klippy_extras/ here
# remains the reviewable mirror; a real deployment always needs both
# commits, in both repos, kept in sync. Pure linear fast-forward on master
# (verified: 845396f0 is a direct ancestor).
KLIPPER_REPO=https://github.com/Klipper3d/klipper.git
KLIPPER_BRANCH=master
# 2026-08-11 (physical qualification session): bumped to 7a755706 - the fail-closed
# multi-read sensor-consistency gate added after live testing found a raw step operation
# (SAFE_MOVE_Z) leaves deal_avgs_prtouch intermittently returning near-zero/partial garbage
# that a single-read guard cannot catch (see docs/NEBULAOS_PRTOUCH_MCU_TIMER_FORENSICS.md's
# physical-qualification follow-up). Direct fast-forward ancestor chain on
# coreflake1/NebulaOS-klipper's master: 4ae82620 -> 31f15cec -> 7a755706.
# ^^^ every line above this point describes the RETIRED fork pin, kept for
# provenance. The live pin is the official-upstream commit below.
#
# 2026-08-17 (Phase 1 no-fork migration): official Klipper3d/klipper
# fe4eb8650b, tagged v0.13.0-734-gfe4eb865, 588 commits past the point the
# retired fork's lineage diverged from mainline. NOT a fast-forward of the
# old pin - it is a different repository, so the usual ancestor-chain note
# does not apply and cannot.
#
# 2026-08-20 (Phase 1.5 hardware closure): rolled back from fe4eb8650b to
# the official v0.13.0 tag (61c0c8d2ef). Moving 734 commits back to the
# last released tag removes two API differences the extensions had to
# bridge (register_serial_response/check_valid_response, both absent at
# v0.13.0 and introduced later). The extensions now use register_response
# (the v0.13.0 API) with runtime detection for forward compatibility.
# Extension suite passes against this commit composed; required_klipper_-
# symbols updated to match. This is the qualification candidate.
#
# 2026-08-23 (Phase 1.8B host-upgrade mission): advanced from v0.13.0
# (61c0c8d2ef) to 58bd67db3, the same official upstream commit the
# native GD32F303 MCU firmware (NebulaOS-klipper-mcu) is built from.
# The v0.13.0 host cannot configure against the native MCU because the
# host/MCU protocol changed between those two commits (observed:
# KeyError: 'bytes_per_report' / Unable to encode: query_analog_in).
# The correct fix is moving the host to match the MCU's upstream base,
# not patching either side. Extensions are re-qualified against this
# commit: required_klipper_symbols updated (register_response ->
# register_serial_response), the dual-API runtime detection already
# present in prtouch_mcu.py now takes its post-c89393cda path, and
# allow_unqualified remains false. NOT a fast-forward from the v0.13.0
# pin - 58bd67db is a descendant of v0.13.0 on the same official
# Klipper3d/klipper master branch.
KLIPPER_PIN=58bd67db3ce1be1951c3e4a6d1156a79903d4edc

# --- NebulaOS Klipper extensions (everything this project actually owns) ---
#
# 2026-08-17 (Phase 1 no-fork migration): the third image-owned source
# component, alongside Klipper and Moonraker. Seeded, migrated, activated,
# validated, locked and rolled back through the exact same machinery as
# those two - it is not a special case with a parallel lifecycle.
#
# Contents: the nine PRTouch/Z modules, tmcstatus, nebulaos_version, the
# compatibility preflight (nebulaos_compat), the GD32 die-temperature module
# (nebulaos_temperature_mcu), five vendored community modules with real
# attribution in that repo's VENDORED.md, and the 15-module / 300-test
# suite. Real filtered history from the retired fork, so per-file git blame
# and the load-cell safety-hardening series both survive.
#
# The pin here and Klipper's pin above are ONE unit. The update supervisor
# validates, records and rolls back the (klipper, extensions) pair together
# and never records a known-good pair until both halves have passed - a
# half-updated pair is exactly where the chelper failure and the
# register_response -> register_serial_response API rename land.
KLIPPER_EXTENSIONS_REPO=https://github.com/coreflake1/NebulaOS-klipper-extensions.git
KLIPPER_EXTENSIONS_BRANCH=main
# 2026-08-20 (Phase 1.5 closure): advanced from 79fa891d to 4bdd419f,
# same "bump the pin on a feature branch without merging to main" pattern
# already used for GUPPYSCREEN_PIN below.  This commit carries:
#
#   - v0.13.0 dual-API support: prtouch_mcu detects register_response vs
#     register_serial_response at runtime via hasattr(), so the same code
#     works on both v0.13.0 and post-c89393cda Klipper.
#
#   - tmcstatus reactor crash fix: get_status() no longer calls
#     reactor.pause(); a periodic timer caches TMC register data instead.
#
#   - Manifest update: qualified_commit points to v0.13.0 tag, required
#     symbols updated (register_response), forbidden list cleared.
#
#   - 10 new tmcstatus regression tests (timer lifecycle, cached status,
#     shutdown, multi-driver errors, sense-resistor passthrough).
# 2026-08-24 (Phase 1.8 safety closure + contact interpolation): advanced
# from 7260389a to b4b0301. Carries: upstream 58bd safety parity closure
# (7 gaps closed), contact interpolation via LCBestFit (matching upstream
# TappingMove semantics), composition manifest updated to include
# nebulaos_z_offset_probe.py and its test suites. 98 safety/coexistence
# tests pass, 0 new regressions. allow_unqualified remains false.
# 2026-08-28 (Phase 1.8B integration candidate): advanced from 389e49f3 to
# 25a99994, same "bump the pin on a feature branch without merging to main"
# pattern as above. Carries: native nozzle-clear activated
# (HARDWARE_BEHAVIOR_BLOCKED=False, after fixing a bare-assert safety gate
# and a missing bed-mesh-suspension gap found in offline review), full
# PRTouch runtime removal (7 production modules + 9 dedicated test files
# deleted; prtouch_test_support.py decoupled from prtouch_mcu.py and kept
# as generic test infrastructure - it's still relied on by
# test_z_offset_probe_safety/test_z_compensate_offset_safety/etc., which
# is why it wasn't simply deleted alongside the rest). z_compensate.py no
# longer references prtouch_v2/prtouch_mcu/prtouch_probe/prtouch_nozzle in
# any way. See extras/PRTOUCH_REMOVAL_PLAN.md and
# _project/missions/phase1.8b-pre-build-review.md for the full review.
# 2026-08-29 (Phase 1.9A): advanced from 9f09b49f to 61cf27c2 - vendors
# bl24c16f.py (the physical BL24C16F EEPROM's generic I2C driver, unmodified,
# from stock's real firmware).
# 2026-08-29 (Phase 1.9B PLR mission): advanced from 61cf27c2 to 3385d55.
# Adds nebulaos_power_loss_recovery.py and its EEPROM journal codec
# (nebulaos_plr_journal.py) - see _project/missions/ for the full design.
# [bl24c16f] itself stays vendored for provenance but is retired from
# production use: machine.cfg no longer instantiates it, and the physical
# EEPROM's production owner is now the Linux 6.6 in-tree at24/nvmem driver
# (see accelerometer-eeprom-bus-enable-variant.sh's own Phase 1.9B update).
# 2026-08-29/30 (Phase 1 overnight closure mission, Mission A): advanced
# from 3385d55 to 093c258. Fixes checkpoint execution semantics - proved
# from the exact pinned Klipper source that toolhead.register_lookahead_
# callback() alone does not prove motion physically executed (only that
# it was scheduled); replaces the single-shot checkpoint with a
# candidate/promotion pipeline gated on mcu.estimated_print_time() real-
# time catch-up, the same proof M400's wait_moves() uses. 10 new tests.
# 2026-08-30 (Phase 1 final candidate cleanup mission): advanced from
# 093c258 to b6ae35e. Declares extras/test_z_offset_probe_down_min_z.py in
# nebulaos-extensions.json's own module manifest - a real, long-standing
# gap (the file existed but was never declared) that made test_nebulaos_
# compat.py's own manifest-completeness check fail on every run. Zero
# runtime/behavioral change - test-manifest only.
# 2026-08-31 (Phase 2 calibration-framework mission, feature branch):
# advanced from b6ae35e to d9483e8, same "bump the pin on a feature branch
# without merging to main" pattern as above - needed to let a real pinned
# build actually compose this slice's extras code (the previous pin
# predates all of Phase 2). Carries: load-cell availability as a preflight
# condition (not a startup requirement) for z_compensate.py's nozzle-clean/
# z-offset commands; the shared probe/nozzle pairing primitive
# (nebulaos_probe_pair.py); the nebulaos_calibration.py coordinator
# (NEBULAOS_Z_OFFSET_CALIBRATE, PID/bed-mesh wrappers,
# NEBULAOS_CALIBRATION_STATUS); and NEBULAOS_AXIS_TWIST_CALIBRATE
# (LOAD_CELL/MANUAL, both axes, integrating with pristine upstream
# axis_twist_compensation.py through its own public surface only - see
# that file's own module docstring). allow_unqualified remains false.
KLIPPER_EXTENSIONS_PIN=d9483e8b09725d3f924c14f8579252ba43c3411f

# --- Moonraker (official, not forked) ---
MOONRAKER_REPO=https://github.com/Arksine/moonraker.git
MOONRAKER_PIN=d5ee17128bb88434aacdab90c2e9e990e2b64e4a

# --- Camera pipeline (real GPLv3 uStreamer port for this board family) ---
K1_USTREAMER_REPO=https://github.com/pellcorp/k1-ustreamer.git
K1_USTREAMER_PIN=18e30bb313d54b1b01dd995bd31ce5a3d5adffd6

# --- v4l2-ctl (diagnostic utility, autotools build, pinned to v1.20.0's
# exact resolved commit rather than the tag name) ---
# Virgin-Baseline Fix + Rebuild mission (2026-08-08): git.linuxtv.org (the
# canonical upstream) had a real, observed outage (502/504) mid-build,
# twice, on two separate fresh-clone attempts. V4L_UTILS_REPO/PIN remain
# the source of truth for identity (never changed by this fix - same
# commit, same project) but 00-fetch-vendor-sources.sh now fetches the
# actual bytes from a deterministic, SHA256-pinned archive instead of a
# live git clone, removing this one third-party server from the build's
# critical path. The archive was built from a mirror (github.com/gjasny/
# v4l-utils) only after independently verifying, via git's own content-
# addressed commit hash, that its copy of this exact commit is byte-
# identical to what git.linuxtv.org itself would serve - see that
# release's own notes for the full verification record.
V4L_UTILS_REPO=https://git.linuxtv.org/v4l-utils.git
V4L_UTILS_PIN=3b22ab02b960e4d1e90618e9fce9b7c8a80d814a
V4L_UTILS_ARCHIVE_URL=https://github.com/coreflake1/NebulaOS-firmware/releases/download/v4l-utils-vendor-src-3b22ab0/v4l-utils-3b22ab0-pinned-src.tar.gz
V4L_UTILS_ARCHIVE_SHA256=e7703e35cba9d03ae09778d99b66c23d855cb31cf9cb58b4d52a35ae544463e1

# --- Mainsail (built Vue app, fetched as a release archive - pinned by tag
# AND a sha256 of the downloaded zip, not a git ref) ---
MAINSAIL_TAG=v2.18.2
MAINSAIL_SHA256=df2ba7c301f7bfc8ac9f122741a6ba08356d679ecfa1f62f898d0337802d5de5

# --- Board NVRAM (Cypress/Broadcom-format, board-specific calibration data) ---
#
# 2026-08-07 (canonical-repository mission): redistribution explicitly
# authorized by the repository owner - see LICENSES/WIFI-FIRMWARE-NOTICE.md
# for the full provenance/license reasoning. Published as a GitHub Release
# asset on this same repo (wifi-firmware-v1.0.0) and fetched the same way
# Mainsail is above: pinned tag + URL + archive sha256, individual file
# hash checked after extraction.
#
# 2026-08-09 (WiFi .125 promotion): this pin now covers ONLY the NVRAM
# (.txt) file. The .bin/.clm_blob pair that used to travel in the same
# release archive (the obsolete 7.46.58.13 control firmware) is retired -
# see WIFI_FIRMWARE_BIN_SHA256/WIFI_FIRMWARE_CLM_SHA256 below, now sourced
# directly from Infineon's own upstream repo instead. The NVRAM itself is
# real per-board calibration data extracted from this project's own
# hardware, unrelated to which firmware build is running, and stays
# byte-identical across this promotion (confirmed live: unchanged on the
# running device both before and after the .125 switch).
WIFI_FIRMWARE_RELEASE_TAG=wifi-firmware-v1.0.0
WIFI_FIRMWARE_RELEASE_URL=https://github.com/coreflake1/NebulaOS-firmware/releases/download/wifi-firmware-v1.0.0/nebulaos-wifi-firmware-1.0.0.tar.gz
WIFI_FIRMWARE_ARCHIVE_SHA256=20c22ee2e3b7469b1240f991457e7217d3b1c8d858595754582e4f097311b08c
WIFI_FIRMWARE_TXT_SHA256=78fee458ab69c0a66ea462f6d6769e15b36f73582693f4dbb5a0e8e8be3cfb0a

# --- Canonical CYW43430 WiFi firmware + CLM (Infineon ifx-linux-firmware,
# 7.45.98.125) ---
#
# 2026-08-09: promoted from the previous 7.46.58.13 control build after
# successful hardware qualification (see
# docs/NEBULAOS_WIFI_125_ENGINEERING_TEST.md's "Correction" section for the
# full incident writeup and live verification evidence). Fetched directly
# from Infineon's own public repo by
# scripts/firmware/fetch-cyw43430-wifi-firmware.sh - see that script's own
# header for the license basis (Cypress Wireless Connectivity Devices
# Driver EULA, same family already accepted for this build's other
# firmware). Pinned to an exact commit (the tag's own dereferenced target,
# not a moving ref) AND individual file sha256 - a version bump means
# re-deriving both deliberately.
WIFI_FIRMWARE_INFINEON_REPO=https://github.com/Infineon/ifx-linux-firmware.git
WIFI_FIRMWARE_INFINEON_TAG=release-v5.10.9-2022_0909
WIFI_FIRMWARE_INFINEON_PIN=4334275b5801bcf5256c3101395e7bc983ce640d
WIFI_FIRMWARE_BIN_SHA256=82ed67a211877efa47aff4aab83d6d2d1ccf3d5d0f5c396df97f292ade01de9e
WIFI_FIRMWARE_CLM_SHA256=1dbe1a396b68786bb189b7c255318ae546fd2e9d15f70ccc8ecbdc52b6cd4c47

# --- GuppyScreen (project-specific fork; frontend consumer of the
# z_compensate structured status contract) ---
#
# 2026-08-07: this is a NEW pin - GuppyScreen was previously not pinned at
# all anywhere in this build. The binary was manually cross-compiled in a
# separate checkout (nebulaos-guppyscreen/) and manually `cp`'d into
# scripts/build/overlay/opt/guppyscreen/ and artifacts/guppyscreen-mips/ -
# see docs/NEBULAOS_QUALIFIED_BASELINE_VARIANT_AUDIT.md for the full
# history of that gap. scripts/build/04-cross-compile-app-stack.sh now
# fetches and builds this pin automatically (see its own GuppyScreen
# section) - the manual-copy step is retired.
#
# Pushed 2026-08-07 (confirmed resolvable from a fresh clone as part of
# this mission's own clean-room reproducibility test).
#
# 2026-08-14 (repository canonicalization mission): bumped from be5d372c to
# b15ad7f. be5d372c's own crash fix (try/catch around Config::init()'s/
# ThemeConfig::init()'s json::parse) wrapped the failure but never changed
# the open mode - json::parse(std::fstream(path)) still opens read+write by
# default, which still fails on this build's actual deployment target
# (rootfs mounted squashfs ro, confirmed live on the golden-reference
# printer), so every boot was silently falling back to in-memory defaults
# and never actually reading the real config.json/theme.json content - no
# crash, but saved settings were never honored either. b15ad7f reapplies
# NebulaOS-guppyscreen's own openke-fixes branch fix (std::ifstream instead
# of std::fstream) by hand on top of be5d372c's try/catch (a straight
# cherry-pick conflicts with be5d372c's rewritten surrounding lines) -
# combines both: real files now actually parse, malformed ones still fall
# back safely. 6/6 tests in test-config-theme-parse-safety pass unchanged.
# NOT YET on the currently-deployed golden-reference printer (that build
# was cut from be5d372c) - this is a known, deliberate delta a fresh build
# from this pin will show relative to that specific running device.
# --- QUALIFICATION-ONLY PIN BUMP (Phase 1 no-fork migration branch) ---
# 2026-08-17. On THIS branch only, GUPPYSCREEN_PIN is advanced from
# b15ad7f6cce9c40b61c0e5f678e3895b989deb13 (an ancestor of, and therefore
# predating, all six Phase 0 GuppyScreen commits) to the current tip of
# NebulaOS-guppyscreen's `phase0/remove-openke-legacy` branch,
# c8c9b0f00b4c4cf08a349d348798ecd25a6d7cb5.
#
# WHY: Phase 0's closeout (_project/missions/2026-08-phase0-safety-cleanup.md
# §A12) proved that a full build from b15ad7f packages a GuppyScreen binary
# that still contains the `wl roam_off` calls Phase 0 deleted - i.e. no build
# to date has ever exercised the COMBINED Phase 0 firmware + GuppyScreen
# content in one image. The combined Phase 0 + Phase 1 qualification effort
# needs exactly that combination, so this branch pins it deliberately.
#
# THIS IS NOT THE PHASE 0 MERGE. NebulaOS-guppyscreen PR #4 is still open and
# unmerged; nothing here merges, approves, or presumes the outcome of it. When
# PR #4 does merge normally, the real pin bump on `main` should point at the
# resulting commit on `main`, not at this branch tip, and this qualification
# comment block should be removed with it.
#
# --- SECOND QUALIFICATION-ONLY PIN BUMP (Phase 1.5 persistent-namespace
# mission, 2026-08-18) --- advanced again, still on a qualification branch
# only, from c8c9b0f (above) to NebulaOS-guppyscreen's
# `phase1.5/persistent-namespace` tip, 5f1911ac938451bb00439e8c55c87ef60b4a1566.
# That branch is stacked on top of c8c9b0f (still-open PR #4) with one
# additional commit fixing hardcoded /usr/data/printer_data/... and
# /usr/data/guppyscreen/... paths in the compiled binary and vendored
# helper scripts to use the already-correct /opt/printer_data/... and
# /opt/guppyscreen/... bind-mount paths instead - this is what let
# NebulaOS-firmware's own S01persistent-datastore stop creating the two
# top-level compatibility symlinks those hardcoded paths used to require.
# See NebulaOS-firmware's own docs/NEBULAOS_PERSISTENT_NAMESPACE.md and
# NebulaOS-guppyscreen PR #5 (stacked on the still-open PR #4).
#
# Pin reachability verified before use, not assumed:
#   git ls-remote origin phase1.5/persistent-namespace
#     -> 5f1911ac938451bb00439e8c55c87ef60b4a1566  refs/heads/phase1.5/persistent-namespace
# Real MIPS cross-build and 66/66 host tests re-verified against this exact
# commit (not reused from the prior pin's build) before it was pinned here.
GUPPYSCREEN_REPO=https://github.com/coreflake1/NebulaOS-guppyscreen.git
GUPPYSCREEN_BRANCH=main
GUPPYSCREEN_PIN=5f1911ac938451bb00439e8c55c87ef60b4a1566
GUPPYSCREEN_VERSION=v1.5.0-OpenKE
GUPPYSCREEN_THEME=blue

# --- pellcorp/k1-bash-build (MIPS cross-compile toolchain container) - RETIRED ---
# Pinned by digest 2026-08-08 (Final Pre-Flash Audit mission), used by every
# 02-06 build stage's nested `docker run` through Migration A. Final Closure
# mission (2026-08-15): retired now that the unified image below is
# promoted and merged to main - no script anywhere in this repo references
# this pin any longer (confirmed by search: only historical-context comments
# in package-deployment.sh, 05-final-build.sh, scripts/build/README.md, and
# FIRMWARE.md's own journal remain, none of them live code). The real digest
# (pellcorp/k1-bash-build@sha256:0b96d1d65175c5a2e3a83a64c3212d08dd774fef09
# 00f991e0ebc570ba896c85) stays recorded in git history (this line's own
# blame) and in FIRMWARE.md for provenance - not reproduced here as a live
# value, so nothing can accidentally source or pull it again by habit.
# CURRENT_BUILD_DEPENDENCY status: NONE.

# --- ghcr.io/coreflake1/nebulaos-build (unified build environment - CANONICAL) ---
# Phase 11 (2026-08-15): replaces the two nested containers pellcorp/
# k1-bash-build (above) and ghcr.io/coreflake1/guppydev with one
# NebulaOS-owned image - see build-env/Dockerfile and build-env/versions.env
# for exactly what it contains and why. build.sh reads and uses this pin
# unconditionally (there is no more "optional" containerized mode - see
# build.sh's own header).
#
# STATUS: CANONICAL. Built from build-env/Dockerfile at the commit noted
# below, pushed to GHCR, and used for the Phase 11 rebuild-and-compare
# experiment against the frozen Phase 9 reference artifacts
# (SEMANTICALLY_IDENTICAL_WITH_NONDETERMINISTIC_METADATA - no unexplained
# product differences). Final Closure mission (2026-08-15) then: hardened
# the verification gates, fixed host-checkout-path leakage into build
# artifacts, ran a genuinely fresh repeat build (succeeded first try,
# SEMANTICALLY_IDENTICAL_WITH_KNOWN_NONDETERMINISM against the original),
# qualified the result on the physical golden printer (boot/WiFi/Moonraker/
# Mainsail/Klipper-MCU/GuppyScreen/camera/G28/PRTouch/Z_OFFSET_CALIBRATION -
# PASS), and merged feature/unified-nebulaos-build-container to main.
# Promoting a future candidate means repeating that same sequence and then
# deliberately updating this section, in its own reviewed commit - never
# automated, never silent.
BUILD_IMAGE_REPO=ghcr.io/coreflake1/nebulaos-build
BUILD_IMAGE_DIGEST=sha256:a6ba57c69fa1ea630b037a1d1f55cf0c044a7f5a403bde9b155ea54bca1cceba
BUILD_IMAGE_CANDIDATE_TAG=candidate-2026-08-15
# Corrected 2026-08-15: was c3083e0 (the unrelated baseline-verification
# fix commit) - the real, only two commits ever touching build-env/
# Dockerfile are bdc3ebb (initial add) and d2f6c85 (the full Migration A
# flattening + the live autoreconf/PATH-shadowing fix, squashed together);
# d2f6c85 is what actually produced the pinned digest below.
BUILD_IMAGE_DOCKERFILE_COMMIT=d2f6c85

# --- qualified baseline (explicit, not auto-selected) ---
# NebulaOS Final Closure mission, Phase B (2026-08-15): assert-baseline-
# config.sh and baseline-difference-gate.sh used to pick their own
# reference via `git tag -l 'nebulaos-canonical-baseline-*' --sort=
# -creatordate | head -1` - better than the hardcoded-stale tag it replaced
# (see the Phase 11 report's own §2), but still implicit: a new tag
# silently becomes the verifier's reference the moment it's pushed, with
# no deliberate promotion step and no record of which tag a given
# verification run actually checked. One explicit value here instead -
# release/qualification promotion means deliberately changing this line,
# in its own reviewed commit, not just pushing a new tag. Both scripts
# fail loudly if this value doesn't name a tag that actually exists in the
# checkout, and both print the exact tag used.
QUALIFIED_BASELINE_TAG=nebulaos-canonical-baseline-2026-08-14-prtouch-qualified
