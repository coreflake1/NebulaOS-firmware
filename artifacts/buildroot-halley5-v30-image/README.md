# Recorded build outputs — not the expected configuration for current source

Everything in this directory except the two `*-fragment.config` files is a
**recorded output of one past build**, committed as evidence of what that build
produced. None of it is regenerated by reading current source, and none of it is
authority for what the current source would build.

Audit finding F-09 exists because `kernel.config` here was being read as if it
described the current configuration. It does not.

## Which files are inputs and which are outputs

| File | Role |
|---|---|
| `halley5-nebulaos-fragment.config` | **INPUT.** Copied into the Buildroot tree by `scripts/build/02-configure-buildroot.sh:90`. Variant scripts rewrite their own marker blocks in it in place, so it is both tracked and build-mutated. |
| `halley5-nebulaos-busybox-fragment.config` | **INPUT.** Same mechanism. |
| `buildroot.config` | Recorded output. |
| `kernel.config` | Recorded output — regenerated from the built kernel's own `.config` by `scripts/build/05-final-build.sh:65`, hashed at `:164`, and read **post-build** by `scripts/build/assert-baseline-config.sh:146,204-211`. |
| `halley5_v30.dts`, `halley5_v30.dtb`, `halley5_v30.decompiled.dts` | Recorded outputs. |
| `build-manifest.txt` | The provenance record for all of the above. |

## When these were recorded

From `build-manifest.txt`:

```
built_at         = 2026-08-28T21:31:56Z
git_commit_main  = 85814b604a6857669d287df72669cc7629896497
git_commit_kernel= 295b7101d751fd888ae39e6f1746a4a940664a5f
```

That build **predates the Phase 1.9A/1.9B host-MCU accelerometer and EEPROM bus
work.** So `kernel.config` here shows `CONFIG_SPI_GPIO`, `CONFIG_EEPROM_AT24` and
`CONFIG_I2C_CHARDEV` all unset, which is *not* what current source builds. The
current expected state is `CONFIG_SPI_GPIO=y` and `CONFIG_EEPROM_AT24=y` with
`CONFIG_I2C_CHARDEV` deliberately not selected — see the
`NEBULAOS_ACCELEROMETER_EEPROM_BUS_ENABLE_VARIANT` block in
`halley5-nebulaos-fragment.config`, `scripts/build/accelerometer-eeprom-bus-enable-variant.sh`,
and the assertions in `scripts/build/assert-baseline-config.sh`.

**Do not hand-edit `kernel.config` to make it agree.** It is generated; the next
build overwrites it. If it looks wrong, that means no build has run since the
source changed — which is exactly what it is currently telling you.

## How to read a MISS from 06-verify

`scripts/build/06-verify.sh` prints `MISS` lines but **exits 0** and gates
nothing. The mechanical gate is `scripts/build/assert-baseline-config.sh`, which
sets `FAILED=1` and exits non-zero, run pre- and post-build by
`scripts/build/build-qualified-baseline.sh:47,58-62`.
