# NebulaOS Mutable Runtime Architecture

**Status: Complete and live-qualified.** This document preserves the chronological investigation and implementation record, section by section, exactly as it was written at each point in the mission. Statements below such as "in progress," "not yet started," "NEEDS_RUNTIME_EVIDENCE," or "NEEDS_DECISION" describe the state at the historical checkpoint where they appear and are superseded by later completion sections (§3.5 onward) and the final implementation report (`docs/NEBULAOS_MUTABLE_RUNTIME_IMPLEMENTATION_REPORT.md`) - they are not the current state of the system.

**Current final state (2026-07-27, closure + final-seal missions):**

```text
/usr/data/nebulaos:
    authoritative mutable runtime namespace

/usr/data/printer_data/gcodes:
    intentional shared G-code exception

/usr/data/openke:
    absent and forbidden as an active runtime path
```

**Scope:** this is the architecture doc for the "NebulaOS Mutable Applications, Updates, Recovery, Shared G-code, and Retention" mission. Section 1 is the original Phase 1 investigation record, written before any implementation began; later sections (§2 onward) document each phase as it was actually built, tested, and closed.

---

## 1. Phase 1 investigation findings (2026-07-26)

### 1.1 Prior planning documents (read in full)

Two planning documents already exist in the sibling GuppyScreen repo (`/home/tim/Documents/guppyscreen/`), written 2026-07-26, read-only static audits, no device/build actions taken while producing them:

- `gap analisys nebulaOS.md` (558 lines) — GuppyScreen↔NebulaOS compatibility gap analysis. Its scope is **GUI/Klipper-extra feature portability** (GuppyScreen panels, Klipper extras, Moonraker API compatibility), not the mutable-runtime/update/retention architecture this mission covers. Still directly useful here for:
  - **Klipper-extras provenance** (its Section 6, G42/G43, and open questions #4/#5) — partially resolved further in this document, Section 1.3 below.
  - **Confirmation that `[spoolman]` is not currently shipped**, that `custom_macro`/`BL24C16F`/`temp_offset_flag`/power-loss recovery have no source anywhere and are explicit product decisions, not technical gaps.
  - Its own recommended architecture (hybrid, mainline-first, thin compatibility shims) is **orthogonal** to this mission — it concerns GuppyScreen panel behavior, not how Klipper/Moonraker/Mainsail themselves get installed/updated/rolled back. No conflict found.
- `suggested fix plan nebulaOS.md` (597 lines) — turns the gap analysis into a GuppyScreen-porting implementation roadmap (path-centralization, seed-config macros, feature gating, hardware-dependent work). Same scope note applies. No update-manager, retention, or mutable-application-layout content found in this document (confirmed by direct keyword search — no hits for "update manager", "mutable", "namespace", "SimpleAF", "retention" as architectural topics, only one incidental "Cleanup/retention service" row about GuppyScreen's own `S45cleanup`/`cleanup-files.sh` porting, which **is** relevant and is folded into Section 1.5 below).

**Conclusion:** both documents satisfy this mission's Phase 1 instruction to read existing gap-analysis/fix-plan material, but neither substitutes for this mission's own investigation — their content is adjacent (GUI porting), not the mutable-runtime/update/retention design this mission must produce.

### 1.2 Current NebulaOS persistent-storage and bind-mount architecture (as of commit `81a4fcf`, the known-good baseline)

Read directly: `scripts/build/overlay/etc/init.d/S01persistent-datastore` (full file).

- `/usr/data` (`/dev/mmcblk0p10`, ext4, physically shared with stock) is mounted at boot by this script (not `S00`).
- Everything this build writes lives under **`/usr/data/openke/`** — the exact top-level name this mission must retire (Section 1.4).
- Only **one** persistent app-data tree currently exists: `/usr/data/openke/printer_data`, seeded once from `/opt/printer_data`'s squashfs defaults, then bind-mounted (`mount --bind`) back over `/opt/printer_data`. This is the **only** bind-mount currently in the system for application data.
- GuppyScreen's own single config file (`guppyconfig.json`) gets a **single-file bind mount** (not a directory) — the only other bind mount in the system — because GuppyScreen hardcodes its config path relative to its own binary location (`/opt/guppyscreen/guppyconfig.json`, read-only squashfs) with no override flag.
- **Klipper, Moonraker, and Mainsail themselves are not persistent at all today** — they are baked directly into the read-only squashfs at `/opt/klipper`, `/opt/moonraker`, `/usr/share/mainsail`. There is no existing bind-mount, versioning, or update mechanism for any of the three. This confirms the mission brief's own framing: Phases 3–7 are genuinely new work, not an extension of an existing partial mechanism.
- Two compatibility symlinks exist guarding against destroying real stock data on the shared partition (`/usr/data/printer_data`, `/usr/data/guppyscreen` — only created if nothing already exists at that path). This defensive pattern (never blindly overwrite existing content on the shared partition) must carry over to the new namespace-creation logic in Phase 3/4.
- **No cleanup/retention script exists anywhere in NebulaOS today** (confirmed by repo-wide `find -iname '*cleanup*'`, only hits were an unrelated dmesg artifact under `artifacts/parity/`). Phase 9's retention manager has **no existing NebulaOS implementation to extend** — it is new work, informed by stock's proven pattern (Section 1.5), not a port of an existing NebulaOS cleanup.

### 1.3 Klipper-extras provenance audit (byte-level, resolves gap-analysis open questions #4/#5)

Diffed directly (`diff`), not inferred:

| Extra | Location | Provenance | Classification |
|---|---|---|---|
| `guppy_config_helper.py` | `klippy_extras/` | **Byte-identical** to `guppyscreen/k1/k1_mods/guppy_config_helper.py` | `required-in-NebulaOS-fork` — GuppyScreen's TMC-autotune/config-CRUD panels depend on it; carried over verbatim from the GuppyScreen community-extra ecosystem, not from `pellcorp/klipper` upstream. |
| `guppy_module_loader.py` | `klippy_extras/` | **Byte-identical** to guppyscreen's copy | `required-in-NebulaOS-fork`, same reasoning. |
| `calibrate_shaper_config.py` | `klippy_extras/` | **Byte-identical** to guppyscreen's copy | `required-in-NebulaOS-fork` (blocked on host-MCU/accelerometer bring-up before it does anything useful, but the file itself is confirmed correct/unmodified). |
| `tmcstatus.py` | `klippy_extras/`, wired into `printer.cfg:41` | Near-identical to guppyscreen's own `k1/k1_mods/tmcstatus.py`, with one real fix (deferred `lookup_object` to `klippy:connect`) guppyscreen's copy lacks (per prior gap analysis, not re-verified byte-for-byte in this pass) | `required-in-NebulaOS-fork` — already wired and in production use. |
| `prtouch_v2.py` + 4 companions (`prtouch_probe.py`, `prtouch_mcu.py`, `prtouch_nozzle.py`, `prtouch_calibration.py`) | `klippy_extras/` | Clean-room rewrite (no Creality source available anywhere), 17 unit tests, **never run against real MCU firmware**, **not wired into shipped `printer.cfg`** (confirmed: `printer.cfg` header explicitly documents this as deferred) | `experimental-unsuitable` for the production-tracked branch until real-hardware validation (per `docs/PRINTER_MAINBOARD_PRECONNECTION_CHECKLIST.md`'s staged protocol) — keep present but unwired, exactly as today. |
| `z_compensate.py` | `klippy_extras/` | New design, calls into `prtouch_v2`, unwired, unvalidated | `experimental-unsuitable`, same disposition as `prtouch_v2`. |
| `gcode_shell_command.py` | **Confirmed present natively** in `vendor/klipper/klippy/extras/gcode_shell_command.py` (`pellcorp/klipper` fork itself) | Upstream, not a NebulaOS or GuppyScreen addition | `already-in-pellcorp-klipper` — resolves gap-analysis open question #4 definitively. No separate vendoring needed for anything depending on this (camera-reload macro, calibration shell-outs). |
| Adaptive bed-mesh (`patch_bed_mesh.py` target) | `vendor/klipper/klippy/extras/bed_mesh.py` | Confirmed present natively (`adaptive_margin` config option, `set_adaptive_mesh()` method, `ADAPTIVE_MARGIN` gcode param, at lines 331/453/463/475/612) | `already-in-pellcorp-klipper` — guppyscreen's `patch_bed_mesh.py` core-patcher is moot for NebulaOS, resolves gap-analysis open question #4. |
| Axis-twist wiring (`patch_probe.py` target) | `vendor/klipper/klippy/extras/probe.py` | Confirmed present natively (explicit `axis_twist_compensation` result-adjustment hook at line 366) | `already-in-pellcorp-klipper` — same resolution. |
| `custom_macro` | Referenced only as a live status object by GuppyScreen; no source in either repo | Creality-proprietary, plaintext on stock, never captured | `obsolete` for now / `NEEDS_DECISION` if GuppyScreen's 3-4 consumed fields are ever worth reimplementing — **out of this mission's scope** (this mission's brief does not ask for `custom_macro`; only listed as a minimum audit candidate because it appeared in the earlier GuppyScreen gap analysis). |

**Net effect on this mission's required Klipper-fork content (Phase 6):** every extra GuppyScreen's *currently-enabled* feature set needs (`tmcstatus`, `guppy_config_helper`, `guppy_module_loader`, `calibrate_shaper_config`, `gcode_shell_command` — already upstream) has now been confirmed either present-and-correct or present-natively-upstream. `prtouch_v2`/`z_compensate` remain correctly excluded from production wiring pending real-hardware validation — this mission does **not** change that; Phase 6 only needs to ensure these files are committed into the NebulaOS Klipper fork's own tracked git history (today they live in **this** repo's `klippy_extras/` and are copied into the vendored Klipper checkout by the build pipeline — they are not part of `vendor/klipper`'s own git history at all, which is exactly the gap this mission's Phase 6 must close).

### 1.4 `openke` naming footprint (Phase 11 input)

Repo-wide case-insensitive search (excluding `vendor/`, `.git/`, `artifacts/`) found `openke`/`OpenKE` in 33 files, spanning: top-level docs (`README.md`, `FIRMWARE.md`, `NETWORKING.md`, `ANALYSIS.md`, `DESIGN.md`, several `docs/*.md`), every numbered build script (`scripts/build/0{0-6}-*.sh`), six init scripts' own comments (`S01persistent-datastore`, `S39wifi`, `S50webcam`, `S55klipper`, `S56moonraker`, `S58guppyscreen`), `nginx.conf`, the udev rule and its helper script, three GuppyScreen-config Python scripts, `moonraker.conf`'s own header comment, `songs.conf`, `supervisorctl`, a Buildroot patch, and `.gitignore`.

The single most consequential runtime occurrence is `S01persistent-datastore`'s own `DATA_ROOT=/usr/data/openke` (Section 1.2) — at the time of this early investigation, flagged as `NEEDS_DECISION` on whether Phase 11 should migrate away from it entirely, coordinated with Phase 3's new `/usr/data/nebulaos` layout. **Resolved in §3.8**: Phase 3 did create the new namespace at `/usr/data/nebulaos` with a one-time migration of `printer_data` content out of the old root, exactly as anticipated here — but `S01`'s own `DATA_ROOT` constant was deliberately kept pointing at `/usr/data/openke`, not renamed, since it's the legacy migration *source*, not new NebulaOS-authored code (full reasoning in §3.8).

Per the mission brief, git history is not rewritten and truthful provenance/license text is preserved — the naming-removal work is scoped to **runtime** paths/services/manifests/user-facing text only, not to historical commit messages or doc sections documenting where NebulaOS came from.

### 1.5 Stock/SimpleAF cleanup-and-retention pattern (direct upstream investigation, Phase 9 input)

Investigated `https://github.com/pellcorp/creality` directly via the GitHub API (current HEAD, 2026-07-26; repo is public, 112 stars, actively pushed same day) — **not** the guppyscreen repo's own docs, which don't contain this.

**`k1/moonraker.conf`** (SimpleAF's real, shipped Moonraker config for this exact board family) confirms the update-manager pattern this mission must replicate:
```
[update_manager]
enable_auto_refresh: False
refresh_interval: 0
enable_system_updates: False        # <- confirms "no general Linux package updater exposed", matches this mission's requirement exactly

[update_manager klipper]
channel: dev
pinned_commit: 386fde4fd38e8eda6999e58bf260eceb00051188   # <- identical pinned commit to NebulaOS's own vendored pellcorp/klipper checkout
report_anomalies: False

[update_manager moonraker]
channel: dev
pinned_commit: abd2026b90d86fb738c6619be3ceefcedee2006c

[update_manager fluidd]
type: web
channel: beta
repo: fluidd-core/fluidd
path: /usr/data/fluidd

[update_manager mainsail]
type: web
channel: beta
repo: mainsail-crew/mainsail
path: /usr/data/mainsail
```
This is real, proven, currently-shipping evidence for exactly the shape this mission asks for: Klipper/Moonraker as `channel: dev` git-checkout updates with a `pinned_commit` compatibility gate; Mainsail as `type: web`/`channel: beta` release-based updates; and `enable_system_updates: False` at the top level so Moonraker never exposes a general OS-package updater. **This mission's Phase 10 should follow this exact shape** (adjusted for NebulaOS's own fork URL and A/B path model), not invent a new one.

**`k1/services/S45cleanup`** delegates to **`tools/cleanup-files.sh`** (shared between the K1 and RPi targets) — fetched and read in full. Concrete, currently-shipping thresholds/behavior this mission's Phase 9 retention manager must compare against before designing its own emergency-gcode-cleanup path (per the mission brief's explicit requirement not to guess this):

- **RTC-before-NTP handling**: on Buildroot (`ID=buildroot` in `/etc/os-release`) only, the script records its own start timestamp and — if that timestamp is before a fixed 2025 reference date — **busy-waits** (`sleep 1s` loop) until the clock has jumped forward by more than 20 minutes (i.e., waits for NTP to actually sync) before doing any time-based (`mtime`) deletion at all. This directly matches this mission's own explicit "account for incorrect boot time before NTP sync" requirement — SimpleAF's exact mechanism (detect epoch-2025 floor, wait for a large forward jump) is the concrete pattern to adapt.
- **Emergency gcode deletion threshold**: `REMAINING_DISK < 1000` (MB) on the data partition — **only then** does it delete `.gcode` files from the shared gcodes root, and only ones with `mtime +7` (older than 7 days), only at `-maxdepth 1` (never recurses into subdirectories — would not touch a USB-mounted subtree or nested folders).
- **Deletion order/exclusions elsewhere**: rolled log files older than 7 days, **except** `moonraker.log`/`klippy.log` (NebulaOS equivalent would also need to exclude `guppyscreen.log` if one exists — SimpleAF's own list includes `grumpyscreen.log`, its GuppyScreen-fork counterpart); old `.override.bkp`/`printer-*.cfg` backup files; backup tarballs older than 7 days **with the newest one always explicitly skipped** even if it's also old (`skipped=false` sentinel pattern, "in case its the only file left"); a `pip`/`root/.cache` purge on every run to free overlay space.
- **Logging**: every deletion (or, in `--dry-run`, every planned deletion) is appended to a `cleanup.log` file at the base data directory; the log itself is truncated at the start of each run.
- **Never touches**: active uploads, USB media (no code path in this script references anything outside `$BASEDIR`, which is `/usr/data` — it never descends into a differently-mounted USB path), or the currently-active print (no print-state check exists in this script at all — **note for this mission's own design**: SimpleAF's script does not itself check print-in-progress state before deleting old gcodes; this mission's own brief explicitly requires never deleting an active print, so NebulaOS's retention manager must add an explicit print-state/active-file check that SimpleAF's own reference script does **not** have — this is a deliberate improvement, not a gap in the investigation).

**Installer-level facts also gathered from `k1/installer.sh`** (not the retention script itself, but relevant context): SimpleAF clones itself to `/usr/data/pellcorp` and refuses to run from anywhere else; Klipper lives at `/usr/data/klipper` (real git checkout) with `/usr/share/klipper` as a compatibility symlink; a Moonraker/Klipper Python venv lives at `/usr/share/klippy-env`; a bundled static `curl` binary is installed to `/usr/bin/curl` because stock has none (independent confirmation of a gap this mission's Phase 2 already knew about); the installer checks remaining disk space before proceeding and aborts below a threshold. None of this is proposed for literal reuse (NebulaOS is a flashed A/B image, not an installer-onto-stock, per the earlier gap analysis's own Section 13 conclusion) — it is recorded here only as further proven-pattern evidence for Phase 2 (tooling) and Phase 9 (retention).

### 1.6 Storage/capacity budget (Phase 1 item 11)

Measured directly (not estimated from git-clone size, which overstates on-device footprint substantially — `vendor/klipper`'s full checkout is 231MB but 205MB of that is `lib/` MCU cross-compilation toolchain content never deployed to the device; only `klippy/`+`config/` — about 4.5MB plus a ~12MB `.git` — is ever relevant on-device):

| Component | Per-version on-device footprint | Two versions (current+previous) | Basis |
|---|---|---|---|
| Klipper (git checkout, `klippy/`+`config/`, no MCU toolchain) | ~5MB + ~12MB `.git` ≈ 17MB | ~22–34MB (worktree-sharing vs. duplicate clones) | Measured (`du -sh`) against this project's own vendored checkout |
| Moonraker (git checkout) | ~1.6MB + ~1.4MB `.git` ≈ 3MB | ~6MB | Measured against this project's own vendored checkout |
| Moonraker Python venv (numpy, matplotlib, and the other pywheels already built by this project's own pipeline) | ~20–40MB | ~40–80MB | Measured: existing prebuilt `numpy-2.4.6-...whl` is 17MB; other already-built pywheels (`apprise`, `ldap3`, etc.) total ~1.7MB; a venv also needs a small amount of interpreter/site-packages overhead beyond the wheels themselves |
| Mainsail (extracted release) | ~13MB | ~26MB | Measured against this project's existing `vendor/mainsail-dist` |
| Update staging (transient, one extra full copy during an in-flight update) | up to ~40MB transient | n/a (transient, freed after activation) | Worst-case sum of one extra Klipper+Moonraker+Mainsail copy |
| Logs/backups (rotating cap, retention-managed) | budget, not measured | ~50–100MB recommended cap | Policy choice, informed by Section 1.5's stock pattern |
| Safety margin | — | ≥200MB recommended | Policy choice |

**Total estimated new footprint for the entire mutable-application architecture: roughly 150–250MB**, against the mission brief's own cited ~2.9GiB currently available on `/usr/data` (48% used of ~5.99GiB). **This is not a binding constraint** — capacity is not a blocker for this mission (no `insufficient capacity` stop condition triggered) — but the retention manager (Phase 9) should still enforce the log/backup caps above rather than relying on the large existing margin, since `/usr/data/printer_data` (~1.2GB) and shared gcode (~1.2GB) already consume roughly half the partition today and are expected to keep growing independently of this mission's own footprint.

**Reproducibility caveat carried over from the fix-plan document**: the most recent build artifact was produced from a dirty working tree (`git_dirty_main=yes` at the time that document was written). This mission's own final image should be built from a clean, tagged commit before any acceptance testing, consistent with the existing `KNOWN_GOOD_PRODUCTION_BASELINE.md` precedent.

---

## 2. Phase 6: NebulaOS Klipper fork (completed 2026-07-26)

Resolved the open decision from the original Section 2 draft: the user authorized live GitHub write access (an already-authenticated `gh` CLI session, account `coreflake1`, `repo` scope confirmed) and asked to proceed.

- Created `coreflake1/NebulaOS-klipper` as a real GitHub fork of `pellcorp/klipper` (`gh repo fork ... --fork-name NebulaOS-klipper --default-branch-only`), matching the existing `coreflake1/NebulaOS-guppyscreen` naming convention. Confirmed the fork's default branch (`jun2025`, upstream's own default — not `main`/`master`) contains the project's pinned commit `386fde4`.
- Created a `nebulaos` branch off that pinned commit in the local `vendor/klipper` checkout, copied this repo's own `klippy_extras/*.py` into `klippy/extras/`, and committed them directly into the fork's tracked history (commit `b3d5ab2`) — with per-file provenance in the commit message (community-extra vs. clean-room, matching Section 1.3's classification table). Pushed to `coreflake1/NebulaOS-klipper` (`nebulaos` branch); `origin` was left pointed at `pellcorp/klipper` upstream for future diffing, with a new `nebulaos` remote added for the fork.
- Updated `scripts/build/00-fetch-vendor-sources.sh` to clone from the fork (`coreflake1/NebulaOS-klipper.git`) pinned at commit `b3d5ab2` instead of `pellcorp/klipper.git` pinned at `386fde4` — the local `vendor/klipper` checkout already matches this exactly (verified: correct branch, correct commit, no re-clone needed).
- Removed the now-redundant `cp "$REPO_ROOT/klippy_extras/"*.py ...` step from `scripts/build/04-cross-compile-app-stack.sh` — the wholesale `cp -r "$VENDOR/klipper/klippy" ...` copy a few lines above it already carries the extras into the overlay now that they're committed into the vendored checkout itself. This is the concrete fix for the mission's own requirement that extras be "committed into the tracked branch, not injected as untracked files post-update." This repo's `klippy_extras/` directory remains the reviewable source of truth for editing these files; changes there should be re-committed into the fork's `nebulaos` branch, not just left as loose files.
- Both modified build scripts pass `bash -n` syntax-checking.

**Not yet done** (deferred, not blocking): a rebuild/reflash to prove the new fetch path produces a byte-identical (or intentionally different only in extras-location) image; that is folded into Phase 2's rebuild since both touch the same build stages.

## 3. Phase 2: rootfs tooling (completed 2026-07-26)

Added to `artifacts/buildroot-halley5-v30-image/buildroot.config`: `BR2_PACKAGE_GIT`, `BR2_PACKAGE_CA_CERTIFICATES`, `BR2_PACKAGE_RSYNC`, `BR2_PACKAGE_LIBCURL` (+`BR2_PACKAGE_LIBCURL_CURL` for the `curl` binary, +`BR2_PACKAGE_LIBCURL_OPENSSL` selecting the already-enabled OpenSSL as its TLS backend — confirmed via `package/git/git.mk:31-37` that Buildroot's own git package auto-enables `--with-curl` once `BR2_PACKAGE_LIBCURL=y`, giving git real HTTPS transport through libcurl+OpenSSL, not a bundled/insecure fallback), and `BR2_PACKAGE_LIBOPENSSL_BIN` (the OpenSSL library itself was already enabled; only the CLI binary was missing). Added `wheel==0.42.0` (pure-Python, universal wheel) to the existing pywheels download-and-flatten-into-site-packages step in `04-cross-compile-app-stack.sh`, since Buildroot's own `package/python-wheel` is host-only (`$(eval $(host-python-package))`, no target-package eval) and can't be selected as a target package at all.

Ran the pipeline (`02-configure-buildroot.sh` → `04-cross-compile-app-stack.sh` → `05-final-build.sh` → `06-verify.sh`). **Caught a real bug via this project's own "never trust exit 0 alone" discipline**: the first pass completed with exit 0 and all of `06-verify.sh`'s existing checks passed, but `openssl` was silently absent from the built squashfs despite `BR2_PACKAGE_LIBOPENSSL_BIN=y` being correctly present in the normalized `.config` — Buildroot's per-package build stamps don't automatically invalidate on a suboption-only config change to an already-built package (a known Buildroot limitation, and the same general class of "config change silently not applied" bug this project has hit before, per `02-configure-buildroot.sh`'s own header comments). Fixed by explicitly forcing `make libopenssl-dirclean && make libopenssl` inside the same `pellcorp/k1-bash-build` container (with the same host-dependency `apt-get install` list `02-configure-buildroot.sh` already uses), then re-running `05-final-build.sh` and `06-verify.sh`. Directly confirmed via `unsquashfs -l` (not just trusting the verify script) that `/usr/bin/git`, `/usr/bin/curl`, `/usr/bin/rsync`, and `/usr/bin/openssl` are all now present in the rebuilt `rootfs.squashfs`, and via `unsquashfs -d` extraction that `/usr/lib/python3.11/site-packages/wheel` (v0.42.0) and `/etc/ssl/certs/*` (CA bundle, ~cert count consistent with Mozilla's bundle via the `ca-certificates` package) are present. `06-verify.sh`'s full existing check suite re-ran clean (no MISS/FAIL lines) after the fix, confirming no regression to previously-verified functionality.

New artifact: `built_at=2026-07-26T17:41:13Z`, `git_commit_main=81a4fcf8...` (still `git_dirty_main=yes` — this mission's own source changes are not yet committed), `rootfs_squashfs_sha256=cf8e563df045c1e8a589735ab2611f9cab1631744f1815931b6c57b274a1b489`, `xImage_sha256=65c0e21401f5b4a81a945b4dec196ebf5746f3c992f6642e7ce00d7fed15f6bb` (kernel rebuilt despite no source change — Buildroot's own `olddefconfig` normalization pass evidently touched a kernel-relevant symbol; `git_dirty_kernel=no` confirms no actual kernel source drift).

**Not yet done**: proving real HTTPS (`git ls-remote https://github.com/coreflake1/NebulaOS-klipper.git HEAD`, `curl --fail --location --head https://api.github.com/`, `wget --spider https://github.com/`) requires either flashing this image to the spare slot and testing on the real device's network, or MIPS emulation of the built binaries offline — deferred to a live-device check (paired with Phase 12's own qualification pass, or sooner if useful to de-risk Phase 7 earlier) rather than assumed working from static presence alone.

## 3.1 Phase 3 pre-check: shared gcode location confirmed live (read-only device query)

Before drafting Phase 3's implementation, checked live on the real device (192.168.0.146, confirmed booted on NebulaOS custom via `uname -r` → `6.6.18-rt23` and the OTA marker → `ota:kernel2`; read-only queries only, no state change) whether `/usr/data/printer_data` — the path the mission brief names as the shared stock gcode location — is real, independent content or something NebulaOS itself created:

- `/usr/data/printer_data` is a **real, substantial, independently-owned directory** (root-owned, `readlink -f` confirms it is not a symlink), containing `certs/`, `comms/`, `config/`, `database/`, `gcodes/` (**1.2GB**, real print files with realistic names/dates), `logs/`, `misc/`, `timelapse/`, `.moonraker.uuid`, `moonraker.asvc` — this is a mature, actively-used GuppyScreen/SimpleAF-style install's own printer_data, genuinely independent of NebulaOS.
- `/usr/data/openke/printer_data` (NebulaOS's own private tree, per `S01persistent-datastore`) exists **separately** — confirming the two really are already-distinct trees today, not aliased or overlapping.
- This confirms `S01persistent-datastore`'s existing defensive guard (`[ -e /usr/data/printer_data ] || ln -s ...`) worked exactly as intended on this device: since real content already existed at that path, NebulaOS's boot never created a symlink there, leaving stock's real data untouched.
- **Net effect**: the mission's shared-gcode design (bind-mount `/usr/data/printer_data/gcodes` → `/opt/printer_data/gcodes`) is validated against real, live evidence, not an assumption — there is real, substantial content to share, and no risk of the "shared" directory actually being NebulaOS's own leftover symlink.
- Directory timestamps of `Mar 1-2 2020` seen elsewhere under `/usr/data/openke/` (e.g. `guppyscreen/`, `wpa_supplicant.conf`) are consistent with this board's confirmed RTC-before-NTP-sync behavior (`docs/NEBULAOS_RETENTION_POLICY.md` §1/§2.6) — an early-boot clock artifact, not evidence of genuinely stale content.

## 3.2 Checkpoint: full Klipper-extras provenance table

Expands §1.3 into the full per-file record the mission requires. Copyright/license evidence gathered by reading each file's own header (not assumed) plus the gap analysis's repo-wide license audit (`gap analisys nebulaOS.md` §15, confirming guppyscreen's top-level `LICENSE` is GPLv3 with no per-file NOTICE for the four community-extra files below).

| File | Original source | Copyright/license | Identical or modified | Why NebulaOS needs it | Enabled in production? | Pellcorp/upstream equivalent? | Experimental? |
|---|---|---|---|---|---|---|---|
| `tmcstatus.py` | Community extra, carried from guppyscreen's `k1/k1_mods/tmcstatus.py` | No per-file header; inherits guppyscreen's repo-wide GPLv3 | **Modified** — real fix: `lookup_object` deferred to `klippy:connect` (guppyscreen's own copy looks it up eagerly, which can race Klipper's own object-registration order) | GuppyScreen's TMC status panel force-subscribes to a `tmcstatus` object unconditionally (`init_panel.cpp:96-119` per gap analysis) | **Yes** — wired at `printer.cfg:41` | Not in `pellcorp/klipper` itself (confirmed: `vendor/klipper/klippy/extras/` has no native TMC-status-shaped module beyond the standard per-driver TMC objects) | No |
| `guppy_config_helper.py` | Community extra, byte-identical to guppyscreen's `k1/k1_mods/guppy_config_helper.py` (confirmed via `diff`, §1.3) | No per-file header; inherits guppyscreen's repo-wide GPLv3 | **Identical** | Backs `_GUPPY_SAVE_CONFIG`/`_GUPPY_DELETE_CONFIG`, used by GuppyScreen's TMC-autotune and config-CRUD panels | Present in `klippy_extras/`, not yet confirmed wired into the shipped `printer.cfg` — **`NEEDS_VERIFICATION`** (grep of the seed config didn't show a `[guppy_config_helper]` section as of this check; functionally inert until added) | Not in `pellcorp/klipper` | No, but currently unwired |
| `guppy_module_loader.py` | Community extra, byte-identical to guppyscreen's copy | No per-file header; inherits guppyscreen's repo-wide GPLv3 | **Identical** | Klipper has no built-in dynamic extras load/unload; GuppyScreen's settings UI depends on this | Present in `klippy_extras/`, wiring into shipped config **`NEEDS_VERIFICATION`** (same caveat as above) | Not in `pellcorp/klipper` | No, but currently unwired |
| `calibrate_shaper_config.py` | Community extra, byte-identical to guppyscreen's copy | No per-file header; inherits guppyscreen's repo-wide GPLv3 | **Identical** | Backs GuppyScreen's input-shaper calibration UI | Present, but functionally blocked on host-MCU/accelerometer bring-up (unrelated, separate mission) — not wired into shipped config | Not in `pellcorp/klipper` | No, but currently unwired pending host-MCU work |
| `prtouch_v2.py` | Clean-room rewrite; own header states "Drop-in rewrite of Creality's compiled `prtouch_v2_wrapper.so`" | New original work (this project); references Creality's `prtouch_v2_wrapper.py` (GPLv3) as the studied reference, not copied source | N/A — new code, not a port | Toolhead probe support; no public Creality source exists to use directly | **No** — explicitly deferred, printer.cfg header documents this; 17 unit tests pass but never run against real MCU firmware | Creality's own closed `.so` is the only "equivalent" and is not usable (closed, no license to redistribute) | **Yes — experimental, correctly excluded from the production update branch until real-hardware validation** |
| `prtouch_probe.py`, `prtouch_mcu.py`, `prtouch_nozzle.py`, `prtouch_calibration.py` | Clean-room rewrites, each header explicitly cites the specific Creality function it studied (`run_step_prtouch`/`safe_move_z`, MCU protocol half, `clear_nozzle()`, `cal_tri_data()`/`get_valid_ch()`) and cross-references `ANALYSIS.md` | New original work; explicitly *not* verbatim ports (each header documents specific intentional behavioral deltas from the studied reference, e.g. dropped dead-code paths) | N/A — new code | Companion modules `prtouch_v2.py` depends on | **No** — same deferred status as `prtouch_v2.py` | Same as above | **Yes — experimental**, same disposition |
| `test_prtouch_calibration.py` | This project's own test suite | "This file may be distributed under the terms of the GNU GPLv3 license" (explicit header) | N/A — new code | Validates `prtouch_calibration.py`'s pure-math functions without needing real MCU/printer hardware | N/A (test-only, not shipped as a runtime extra) | N/A | N/A |
| `z_compensate.py` | New design; own header states "NEW code, not a port — Creality's real `z_compensate_wrapper.so` has no published source anywhere" | New original work; design inferred from observed behavior (documented in `ANALYSIS.md` §7), not from any Creality source | N/A — new code | Per-print auto-Z-offset; no public Creality source exists | **No** — explicitly deferred alongside `prtouch_v2` | Creality's closed `.so`, not usable | **Yes — experimental**, same disposition |

**Correction to §1.3's classification discipline**: two files (`guppy_config_helper.py`, `guppy_module_loader.py`) were previously stated as `required-in-NebulaOS-fork` on the basis that GuppyScreen's *feature set* needs them — but a direct check of the shipped `printer.cfg` did not confirm either is actually wired into a `[guppy_config_helper]`/`[guppy_module_loader]` config section today. Per this checkpoint's own instruction ("do not classify an extra as production-required merely because it was present in the previous build pipeline"), both are downgraded here to **present-but-unconfirmed-wiring** rather than asserted production-required, pending an explicit config check during Phase 7 (when GuppyScreen's actual dependency set gets re-validated against the mutable Klipper checkout).

**Experimental-module boundary enforced going forward**: `prtouch_v2.py` and its four companions, plus `z_compensate.py`, remain present in the `nebulaos` branch's `klippy/extras/` (so they're available if/when real-hardware validation happens) but **not** referenced by any shipped `printer.cfg` section, and this mission does not change that. They are correctly outside the "production-required" set despite being committed to the tracked branch — presence in the fork's git history and being wired into production are two different things, and only the latter defines "production-required" per this checkpoint's own instruction.

## 3.3 Checkpoint: immutable factory Klipper pin

The immutable factory Klipper (baked into the read-only squashfs at `/opt/klipper`, used both as the running system's own Klipper today and as Phase 4's future first-boot/recovery seed) is already a **fixed, non-moving commit pin**, not a tracked branch head — confirmed directly:

- Repository: `https://github.com/coreflake1/NebulaOS-klipper.git`
- Branch the commit lives on: `nebulaos`
- Exact pinned commit: `b3d5ab2b9484f1558586c3a2ea43d46ff9a473a7`
- Commit date: `2026-07-26T19:27:15+02:00`
- Tree hash (content fingerprint): `0d0564c0355780d62666cb8e1d895371ea131d56`
- Ancestry confirmed: `b3d5ab2` is exactly `386fde4` (the long-standing `pellcorp/klipper` base this project has always pinned) **plus one commit** (the klippy_extras addition, §2) — verified via `git merge-base --is-ancestor` and `git log 386fde4..b3d5ab2` showing only that one commit. Nothing else changed underneath it.
- Compatibility level: identical Klipper core behavior to the previously-qualified `386fde4` baseline (webcam/MCU-recovery missions were validated against exactly this base) — the only difference is the addition of inert-until-wired extras files, none of which are referenced by the shipped `printer.cfg`.
- This pin is fetched via `clone_pinned` (`scripts/build/00-fetch-vendor-sources.sh`) which does `git checkout <exact-hash>`, not a branch checkout — so a future push to the fork's `nebulaos` branch (e.g. Phase 7's mutable checkout advancing it further) does **not** silently move this factory seed forward. Advancing the factory seed to a newer commit is a deliberate, separate action (re-pinning the hash in that script), matching the mission's explicit requirement to distinguish the immutable factory source from the mutable, Moonraker-Update-Manager-tracked checkout.

## 3.4 Phase 3: shared-gcode detailed validation (live, read-only)

Deeper inspection of `/usr/data/printer_data/gcodes` on the real device (192.168.0.146, custom/NebulaOS booted, read-only queries only), per the mission's explicit pre-activation checklist:

- **Ownership/permissions**: every file and directory root-owned, `644`/`755` — matches NebulaOS's own existing convention (no separate stock UID/GID to reconcile).
- **Subdirectories**: exactly one, `.thumbs` (Moonraker's own standard thumbnail-cache convention — not stock-specific, no compatibility concern).
- **Hidden files**: none besides `.thumbs` itself; no partial-upload or temp-file artifacts present at inspection time.
- **Symlinks**: exactly one — `usb -> /tmp/udisk` (lowercase). This is **stock's own** USB-exposure mechanism (the `ubus`-based auto-mount daemon populating `/tmp/udisk`, per the gap analysis's Section 10) — a dangling, harmless symlink once viewed from NebulaOS (NebulaOS has no `/tmp/udisk` populator; nothing reads this symlink on the NebulaOS side).
- **Real, concrete finding requiring a Phase 5 design decision**: NebulaOS's own USB integration uses a differently-named, differently-mechanized path — a real directory `USB` (**uppercase**, created by `91-usb-gcode-media.rules`/`usb-gcode-media.sh` as the udev mount target). Confirmed live: this `USB/` directory **exists today only inside NebulaOS's current private tree** (`/usr/data/openke/printer_data/gcodes/USB`, currently bind-mounted to `/opt/printer_data/gcodes`) and **does not exist at all** inside the real shared stock tree (`/usr/data/printer_data/gcodes` has no `USB` entry, confirmed via direct existence check). Since ext4 is case-sensitive, `usb` (stock's symlink) and `USB` (NebulaOS's mount-target directory) do not collide as names — but once Phase 5's activation manager bind-mounts the shared stock tree over `/opt/printer_data/gcodes` (replacing today's private-tree gcodes mount), NebulaOS's own `USB/` mount-target directory must be created **inside the shared stock tree itself** (`/usr/data/printer_data/gcodes/USB`) as an idempotent one-time step, or the udev rule's mount target (`/opt/printer_data/gcodes/USB/$DEV`) will not exist post-cutover and USB auto-mount will silently break. This directory would then be a permanent (usually-empty) addition visible to **both** stock and NebulaOS when either boots — architecturally low-risk (analogous to `.thumbs` already being a tolerated extra entry, and it never collides with stock's own lowercase `usb` symlink), but must be explicitly created by Phase 5's activation logic and explicitly exercised in Phase 12's real-hardware USB-hotplug scenario, not merely assumed to work.
- **Active-print/partial-upload state**: none observed at inspection time (no in-progress upload artifacts) — the retention manager's own active-print/partial-upload guards (`docs/NEBULAOS_RETENTION_POLICY.md` §2.4) remain the enforcement mechanism for this at runtime, this was just a point-in-time confirmation that the directory's ordinary contents are unremarkable.

**Conclusion**: the shared-gcode design is validated as safe to implement as designed, with one concrete addition required at Phase 5 (create `USB/` inside the shared tree during activation, not assumed to pre-exist).

## 3.5 Phase 3 implementation: NebulaOS namespace + Phase 4: offline factory seeding

Implemented (not yet built into a flashed image at the time of writing this section — see §5 for the build/flash/verify status):

- **`scripts/build/overlay/etc/init.d/S02nebulaos-namespace`** (new): idempotently creates the full `/usr/data/nebulaos/{apps/{klipper,moonraker,mainsail,guppyscreen},envs/moonraker,printer_data/{config,logs,database,certs,comms,timelapse,misc},system,updates/{staging,locks},backups/{klipper,moonraker,mainsail,printer_config},maintenance/logs}` layout, writes a `system/layout-manifest.json` (schema/compatibility version, shared-gcode path recorded), and performs the controlled, development-only migration of `config`/`database` out of `/usr/data/openke/printer_data` — verified by per-file SHA-256 comparison after copy, guarded by a marker so it never re-runs (and never overwrites) once clean, and **never deletes or renames the old `/usr/data/openke` tree**. Runs after `S01persistent-datastore`; does **not** touch any existing bind mount or running service — `S01`'s current mounts stay exactly as they are.
- **`scripts/build/overlay/etc/init.d/S03nebulaos-factory-seed`** (new): first-boot, fully offline seeding of `/usr/data/nebulaos/apps/{klipper,moonraker,mainsail}` from the immutable bundles/copy at `/opt/nebulaos-seeds` (below). Idempotent (checks for `.git`/`index.html` presence before doing anything), rejects and cleans up on any verification failure (wrong branch after clone, incomplete Mainsail copy) rather than leaving partial state, and records the very first known-good version metadata (`system/known-good.json`) for Phase 8 to consume. Also does not cut any service over.
- **`scripts/build/04-cross-compile-app-stack.sh`**: builds the actual offline seeds — `/opt/nebulaos-seeds/{klipper.bundle,moonraker.bundle,seed-manifest.json}`. **Found and fixed a real git limitation during implementation, before it could waste a multi-hour rebuild**: both `vendor/klipper` and `vendor/moonraker` are shallow clones (`00-fetch-vendor-sources.sh`'s `clone_pinned`); a direct `git bundle create out.bundle <branch>` against a shallow repo produces a bundle `git bundle verify` reports as fine, but a real `git clone` of it fails (`did not send all necessary objects`) — proven locally with a throwaway clone before touching the build pipeline. Fixed via a `make_flat_bundle()` helper that flattens each vendor checkout into a single synthetic orphan commit (identical tree, no shallow boundary) in a throwaway copy, on a branch named to match the real branch (`nebulaos`/`master`), bundling `HEAD` **and** the named branch ref together (bundling the branch name alone landed clones in detached HEAD instead of a real branch — also found and fixed by direct local testing). The seed manifest records both the synthetic `seed_commit` actually shipped and the `true_upstream_commit` it was snapshotted from, so the provenance trail stays honest. **Consequence recorded in `docs/NEBULAOS_UPDATE_AND_ROLLBACK_DESIGN.md` §4.1**: the seeded checkouts have no shared ancestry with the real GitHub history, so the first real update must `fetch`+`reset --hard`, never merge/pull.

**SUPERSEDED 2026-07-28 (auto-updates-camera-complete mission)**: the "no shared ancestry" consequence above was not just a `fetch`/`reset --hard` inconvenience — it made Moonraker's own `git merge-base --is-ancestor HEAD origin/<branch>` check permanently fail, setting `diverged=true` / `is_valid=false` and blocking every real Klipper/Moonraker update on every freshly-seeded device, discovered during live update qualification. `make_flat_bundle()` and the `.bundle` seed format described above were replaced with `make_seed_archive()` (`scripts/build/lib/make-seed-archive.sh`), which tars up each vendor checkout's real `.git` directory and working tree as-is (real shallow boundary, real commits, real branch) instead of flattening into a synthetic orphan commit — `S04nebulaos-factory-seed` (renamed from `S03` at some point after this section was written) now extracts these `.tar` archives directly rather than `git clone`-ing a bundle. Full root cause and fix: `docs/NEBULAOS_MOONRAKER_UPDATE_AND_CAMERA_ANALYSIS.md` §28.
- Mainsail needs no bundle (not a git repo) — the existing `/usr/share/mainsail` static-build copy is already its own valid offline seed; `S03` just `cp -a`'s it.

## 3.6 Phase 5: activation manager implemented

**`scripts/build/overlay/etc/init.d/S04nebulaos-activate`** (new) — the single authoritative activation manager. Design principle: doing nothing is always safe, since every service already runs correctly against its immutable `/opt/*`/`/usr/share/mainsail` path — activation only ever *adds* a bind mount on top once a persistent copy has positively validated (exists, resolves inside the namespace with no traversal/symlink-escape, has its expected marker file, is root-owned, has no update-lock present); any single failed check just means that one app stays on immutable, never aborts the whole boot. Runs after `S02`/`S03`, before `S39wifi`/`S50webcam`/`S55klipper`/`S56moonraker`/`S58guppyscreen`. Handles printer_data + the shared-gcode mount in the required order (private tree first, then the shared stock gcodes tree nested inside it), and creates the `USB/` directory inside the shared tree per the §3.4 finding. Records every decision to `system/activation-state.json`.

**Phase 0 safety/logging cleanup addendum (2026-08-16): renumbered since this section was written.** Same class of drift as the `S03`→`S04` factory-seed rename already noted in §3.5 — `S04nebulaos-activate` was later renumbered to `S05nebulaos-activate` (current on-disk name, confirmed against `scripts/build/overlay/etc/init.d/`), consistent with the `S05nebulaos-activate` reference already used later in this same document (§3.8's table). This section's own body text below is left as `S04`, matching this document's stated policy (§ top) of preserving each section exactly as written at its historical checkpoint rather than retrofitting later renames into old prose. (§3.10's own file listing still reads `S04nebulaos-activate` too — same historical-preservation policy, not a second unfixed drift.)

**Two real bugs found and fixed by testing directly against the live device before trusting the script**, following this project's own "prove it, don't assume it" discipline:
1. `stat -c '%u:%g'` (the obvious ownership-check command) does not exist on this image's BusyBox build at all (`stat: not found`, confirmed live) — fixed by switching to `ls -ldn` (BusyBox's numeric-UID/GID `ls` extension) piped through `awk`.
2. The symlink-escape rejection logic (`readlink -f` + a `case` prefix match against `$NEBULAOS_ROOT`) was tested live against a real symlink pointing outside the namespace (`ln -sf /etc .../apps/evil`) and confirmed to correctly reject it, rather than trusting the logic on paper.

**Not yet done**: this script has not yet been baked into a built image or exercised through an actual boot (see §5 for build status) — the bind-mount qualification scenarios the mission requires (missing/empty/incomplete persistent dir, incompatible metadata, corrupted source, stale mount, rollback transition, A/B switching, shutdown/reboot, nginx/Moonraker/Klipper restart, USB hotplug) are Phase 12 real-device work, not yet performed. The individual command-level building blocks (ownership check, escape rejection, mount-presence check) have been verified live; the integrated script's full boot-time behavior has not.

## 3.7 Phase 9 (retention manager) and Phase 10 (Update Manager config) implemented

**`scripts/build/overlay/etc/nebulaos-retention.sh`** + **`.../init.d/S45nebulaos-cleanup`** (new): namespace-restricted retention manager, invoked backgrounded at boot (matching SimpleAF's own real `S45cleanup` convention). Adapts SimpleAF's real thresholds/behavior (`docs/NEBULAOS_RETENTION_POLICY.md` §1) rather than porting them: normal cleanup only ever touches an explicit allowlist under `/usr/data/nebulaos` (rotated logs, old config backups, abandoned staging, obsolete versions beyond current+previous), every path is resolved and checked against the namespace boundary before deletion, an update-lock directory is checked before running (restricts to log-rotation-only if an update is mid-flight), and emergency shared-gcode cleanup only fires under a genuine critical free-space floor with a real active-print/partial-upload/USB protection check that SimpleAF's own reference script does not have at all.

**Three real BusyBox-vs-GNU-coreutils gaps found and fixed by testing directly against the live device**, not assumed from the script alone:
1. `find -printf` is entirely unsupported (`find: unrecognized: -printf`, confirmed live) — used for sorting backups by age; fixed by switching to `ls -1t` (BusyBox `ls`'s own newest-first sort).
2. Same `-printf` gap in the "is this file still being uploaded" check; fixed with `find -mmin -1` (confirmed supported live) instead of manual epoch-time arithmetic.
3. **The active-print protection check's own JSON pattern was wrong until tested against a real Moonraker response**: `/printer/objects/query?print_stats` returns compact JSON with no space after `:` (confirmed live: `{"result":{"status":{"print_stats":{"filename":"","state":"standby"...`), not the spaced `"state": "printing"` form the check was first written against — fixed to match the real format. This is exactly the kind of silent, never-triggers bug this project's "prove it against the real device" discipline exists to catch.

**Phase 10**: added `[update_manager]` + `[update_manager klipper]`/`[update_manager moonraker]`/`[update_manager mainsail]` to `moonraker.conf`, shaped directly on SimpleAF's own real config (`docs/NEBULAOS_UPDATE_AND_ROLLBACK_DESIGN.md` §1) — `enable_system_updates: False` identical; Mainsail as `type: web`/`channel: beta`. Deliberate difference: no `pinned_commit` on Klipper/Moonraker (this mission's own explicit goal is user-updatable components; compatibility control comes from the two-stage health-check/rollback design instead of a hard pin — this was an open decision in the design doc, now resolved and implemented). No `[update_manager guppyscreen]` section exists at all, matching the mission's explicit scope boundary.

**Not yet done**: Phase 7's actual writable Klipper/Moonraker/Mainsail installs and the real Moonraker virtualenv at `/usr/data/nebulaos/envs/moonraker` this config references don't exist on a running device yet (only the seeded copies from S03, not yet activated+exercised through a real update cycle) — Update Manager configuration is necessarily ahead of having something live to manage; it will be exercised for real during Phase 12.

## 3.8 Phase 11: OpenKE runtime-naming classification (complete)

Repo-wide search (39 files, excluding `vendor/`/`.git/`/`artifacts/`) classified as follows, per the mission's own required distinction:

| Category | Examples | Action |
|---|---|---|
| **Legitimate historical/provenance** — describes a specific past change, mission, or investigation, factually true as written | `# OpenKE addition (FIRMWARE.md sec 13)...`, `# OpenKE app-stack addition...`, most `docs/*.md` mission-status/audit files, `ANALYSIS.md`/`DESIGN.md` | **Not changed** — rewriting these would falsify the historical record. Matches the mission's own explicit instruction. |
| **User-facing/current-identity, safe to rename now** | `songs.conf`'s two comments ("OpenKE buzzer songs" / "reinstall or update OpenKE"), `moonraker.conf`'s top-of-file identity header | **Renamed to NebulaOS** this pass (both low-risk, comment-only, no behavior change). |
| **Internal build-path naming family** | `board/halley5-openke-overlay` → `board/halley5-nebulaos-overlay`, `halley5-openke-fragment.config` → `halley5-nebulaos-fragment.config`, `halley5-openke-busybox-fragment.config` → `halley5-nebulaos-busybox-fragment.config`, `halley5-openke-wheels` → `halley5-nebulaos-wheels`, `.openke-build.lock` → `.nebulaos-build.lock` | **Renamed** across all real source-of-truth files (`.gitignore`, all six numbered `scripts/build/0*.sh` stages, `python-matplotlib.mk`) and the two git-tracked artifact config files themselves (`git mv` + internal path fix), followed by a full clean rebuild (`02`→`03`→`04`→`05`→`06`, since the Buildroot fragment/overlay path identifiers changed) and a real device flash-and-boot to confirm zero regression. The purely-generated `vendor/buildroot-x2000/board/halley5-openke-*` directories were never hand-edited — they're gitignored output `02-configure-buildroot.sh` recreates under the new name automatically. `artifacts/parity/baseline/baseline-artifact-hashes.txt` was deliberately left referencing the old filename — it's a frozen historical snapshot of a specific past build, not live build tooling, and rewriting it would misrepresent what that baseline actually contained. |
| **The one genuine runtime-path occurrence, deliberately NOT renamed** | `S01persistent-datastore`'s `DATA_ROOT=/usr/data/openke` | **Kept as-is, permanently, not just deferred.** This is the literal on-disk directory name a live, previously-provisioned device (192.168.0.146) already has real seeded/migrated data under — `S02nebulaos-namespace`'s own `OLD_ROOT=/usr/data/openke` explicitly treats it as the pre-NebulaOS migration *source*, distinct from the new `/usr/data/nebulaos` namespace it migrates *into*. Renaming it would be purely cosmetic (an already-superseded legacy path being renamed for branding, not because it's new NebulaOS-authored code) while carrying real risk: a device that already completed migration has this path's data effectively vestigial for printer_data (superseded by `S05nebulaos-activate`'s own bind mounts) but still load-bearing for the one thing `S01` uniquely does that Phase 3-5 don't cover - GuppyScreen's single-file `guppyconfig.json` bind mount, which stays out of the NebulaOS namespace by explicit mission scope. Keeping the name `openke` here is a feature, not a gap: it makes it visually obvious in the code that this is the legacy/pre-NebulaOS root being migrated away from, not new namespace code that should carry NebulaOS branding. |
| **Migration-only** | `docs/HOW_TO_SWITCH_STOCK_AND_CUSTOM.md`, `.gitignore` comments | Reference existing/historical paths accurately; no change needed independent of the build-path family above. |

**Honest status**: Phase 11 is **complete**. All safe, no-behavior-change renames are done, the internal build-path family is fully renamed and rebuild-proven, and `S01`'s `DATA_ROOT` was evaluated and deliberately kept unchanged for a documented, permanent reason rather than left as an open question.

**Closure mission update (2026-07-27): the decision above was revisited and reversed.** The reasoning in the table row above was sound at the time it was written, but rested on a premise a later mission brief explicitly corrected: there is no deployed OpenKE user base whose already-migrated on-disk data this needed to protect. With that constraint lifted, `S01persistent-datastore`'s `DATA_ROOT` and `S39wifi`'s `CONF` (the two real remaining runtime dependents on `/usr/data/openke`) were relocated to `/usr/data/nebulaos/...`, `S02nebulaos-namespace`'s one-time `migrate_openke_data()` function was removed entirely, and `/usr/data/openke` itself was removed and proven never recreated across two consecutive cold boots. See `NEBULAOS_MUTABLE_RUNTIME_IMPLEMENTATION_REPORT.md` §4 item 4 and `NEBULAOS_PHASE12_QUALIFICATION.md` for the live verification record.

## 3.9 Checkpoint item 2 completed: clean-build reproducibility proven (with a second real finding)

Ran a genuinely clean-tree rebuild (`make clean` inside the Buildroot tree, which turned out to wipe the host toolchain too, not just target packages/images) to prove the libopenssl dirclean fix (§3, checkpoint) holds without any manual one-off intervention — and it caught a **second, distinct real bug in the process**, exactly demonstrating why this checkpoint item was worth doing rather than assuming exit 0 meant success:

- The abbreviated pipeline order used for both this session's builds (`02-configure-buildroot.sh` → `04-cross-compile-app-stack.sh` → `05-final-build.sh` → `06-verify.sh`, skipping `03-build-kernel-and-rootfs.sh` since the kernel hadn't changed) is **only safe when a Buildroot host toolchain already exists from a prior session** — true for this project's normal day-to-day builds, but false on a genuinely clean tree. `04-cross-compile-app-stack.sh` **correctly detects this itself** (`"Buildroot toolchain not built - run 03-build-kernel-and-rootfs.sh first"`, a real, pre-existing guard in that script — not a bug in the numbered pipeline scripts themselves) and exits immediately, before even reaching its own plain (non-cross-compiling) `cp -r` steps for Klipper/Moonraker/Mainsail.
- **The actual gap was in this session's own ad hoc combined test-runner script**, not the project's real pipeline: chaining `bash 02...; bash 04...; bash 05...; bash 06...` inside one `{ }` block with no exit-code check between stages meant `04`'s clean failure did not stop `05`/`06` from running anyway — so `06-verify.sh` correctly reported a wall of `MISS` lines (`/opt/klipper/klippy/klippy.py`, `/opt/moonraker/moonraker/server.py`, `/usr/share/mainsail/index.html`, `ustreamer`, and more) for content that was never populated, while the combined script's own final `exit=0` said otherwise — a direct, concrete demonstration of this project's own "never trust exit 0 alone" rule.
- **Fix applied**: re-ran `04-cross-compile-app-stack.sh` (now succeeding, since `05`'s own full `make` had, by then, built the toolchain from scratch as a side effect of building the kernel/target packages) followed by `05-final-build.sh` and `06-verify.sh` again. Result recorded in §5 once complete.
- **Process conclusion for future maintainers**: on a genuinely clean tree (or after any `make clean`/`distclean` inside `vendor/buildroot-x2000`), the real required order is `02` → `03` → `04` → `05` → `06`, never skipping `03` — `04`'s own guard will refuse to proceed otherwise, which is the correct, safe behavior; the mistake here was in this session's own shortcut test script, not the tracked pipeline.

## 3.10 Third finding from the same reproducibility exercise: overlay staleness

After the `04`-toolchain-ordering fix (§3.9) completed cleanly (`06-verify.sh` reported zero `MISS` lines for the first time), a direct `unsquashfs -l` check — done anyway, per this project's own "never trust exit 0 alone" rule, rather than stopping at a clean verify run — found the new `S02nebulaos-namespace`/`S03nebulaos-factory-seed`/`S04nebulaos-activate`/`S45nebulaos-cleanup`/`nebulaos-retention.sh`/`nebulaos-healthcheck.sh` files **entirely absent** from the built image, even though `/opt/nebulaos-seeds/*` (added by `04`, not `02`) was present and correct.

**Cause**: those six files were all written *after* this reproducibility build's own `02-configure-buildroot.sh` had already run and synced `scripts/build/overlay/` into Buildroot's board directory — exactly the class of mistake `02-configure-buildroot.sh`'s own header comment already warns about ("always re-run this script after ANY change to scripts/build/overlay/... a rebuild after only touching the template, without re-running this first, silently uses whatever this script last copied there"). This session's own build activity briefly fell into precisely the trap that comment was written to prevent.

**Fix**: re-ran the full `02` → `04` → `05` → `06` sequence again. `02`'s own `rm -rf`+`cp -r` resync picks up the current overlay template (now including all six new files); `04` had to run again immediately after because `02`'s `rm -rf` on the whole board overlay directory also wipes everything `04` itself adds directly (Klipper/Moonraker/Mainsail/ustreamer/the seed bundles) — none of which live in the tracked `scripts/build/overlay/` template. Result recorded in §5 once complete.

## 3.11 Reproducibility exercise: final verified result

After the three real findings above (§3.9 toolchain-ordering, §3.10 overlay staleness) were each fixed and the pipeline re-run, a final `02` → `04` → `05` → `06` pass completed with **zero `MISS` lines** and was independently confirmed via direct `unsquashfs -l` (not the verify script's own say-so) to contain: `git`/`curl`/`rsync`/`openssl` binaries, the CA bundle, `wheel`, `/opt/klipper/klippy/klippy.py`, `/opt/moonraker/moonraker/server.py`, `/usr/share/mainsail/index.html`, `/usr/bin/ustreamer`, and all six new NebulaOS init scripts/helper scripts (`S02`/`S03`/`S04`/`S45nebulaos-*`, `nebulaos-retention.sh`, `nebulaos-healthcheck.sh`) plus the `/opt/nebulaos-seeds/*` factory-seed bundles.

**Final verified artifact** (this is the image to flash for live qualification):
```
built_at=2026-07-26T18:38:32Z
git_commit_main=025a802305da438a7d4456d0e19a0949411b4390 (git_dirty_main=yes - later commits landed after this build)
git_commit_kernel=f7ff80a8aa21886a32783dab167e451298c60a8d (git_dirty_kernel=no)
xImage_sha256=8449c3029bef1c749dc2ffeb110e0b6c01246838213f0beaab031021295611d1
rootfs_squashfs_sha256=cbcb2bc9ba829ec444c89139ce066f5f07e7b5307d994027c2b1993425642941
```

**Checkpoint item 2 is now genuinely complete**: not only was the original stale-stamp bug root-caused and fixed in the tracked pipeline, but the fix (and the whole current overlay) has been proven to hold from a truly clean Buildroot tree — catching two further real, independent bugs along the way (the `03`-before-`04` toolchain ordering requirement, and overlay-resync staleness), both fixed, both re-verified directly against the built squashfs rather than trusted from an exit code.

## 3.12 Live qualification: flash + first boot — mostly successful, one real blocker found

Flashed the verified image (§3.11) to the spare custom slot (`/dev/mmcblk0p6`/`/dev/mmcblk0p8`) from stock, following the mission's strict safety discipline throughout (every safety query and every state-changing command issued as separate SSH invocations, reviewed before proceeding): safety-checked the running custom system (standby, unhomed) → switched to stock → safety-checked stock → transferred `xImage`/`rootfs.squashfs`/`build-manifest.txt` (a truncated first transfer was itself caught by hash mismatch before flashing — the SSH helper's default timeout was too short for the 92MB rootfs; retried with a longer timeout and re-verified the hash matched exactly) → ran `flash-spare-slot.sh` (only after explicit user confirmation, since writing raw partitions is exactly the class of action the session's own safety classifier correctly declined to approve unprompted) → both images write-verified via MD5 → safety-checked stock again → switched the OTA marker to custom and rebooted.

**First boot of the new image — confirmed via live, read-only queries:**
- `klippy_state: ready`, `klippy_connected: true`, zero failed Moonraker components — full production readiness, first try.
- All six new NebulaOS scripts present and executed: `/usr/data/nebulaos/{apps,backups,envs,maintenance,printer_data,system,updates}` fully created by `S02` exactly as designed; `system/layout-manifest.json` written correctly.
- `S04`'s activation manager ran and correctly activated `printer_data` and the shared-gcode mount to `persistent` (bind-mounted from the new namespace) while correctly rejecting Klipper/Moonraker/Mainsail as `immutable:incomplete_or_invalid` (see below for why) — the safety-default design (§3.6) worked exactly as intended: a partially-seeded app was correctly kept on immutable rather than activated in a broken state.
- **Zero regressions to frozen functionality**: printer safe (standby, unhomed), webcam captured a real snapshot, USB still auto-mounted (`sda` visible under `/opt/printer_data/gcodes/USB`), OTA marker remained `ota:kernel2` (confirming `S99confirm-good` validated this exact boot and did not revert to stock).

**Real blocker found: `git clone` is killed by the kernel OOM-killer on this device, which has no swap at all.**
- `free -m`: only 208MB total RAM.
- `dmesg` directly confirms: `Out of memory: Killed process NNNN (git) total-vm:56472kB, anon-rss:37480kB...` — twice, independently (once during the real boot's own `S03` run, once during a manual reproduction of the exact same clone command afterward) — for a clone of an 11.5MB bundle needing only ~37-56MB total-vm. This is not a fluke.
- `swapon /usr/data/swap` (the 128MB swap file stock itself already provisions on this same shared partition, confirmed present at `/usr/data/swap`) returns `Function not implemented` — **this kernel has no `CONFIG_SWAP` support compiled in at all**, so there is no way to absorb this kind of transient memory spike even though the storage for it already exists.
- **Consequence**: `S03`'s seeding left `.partial` directories behind for all three components (Klipper, Moonraker, **and Mainsail** — the Mainsail case has no git involved at all, just `mkdir -p`+`cp -a`, so the OOM pressure is a general low-memory condition at that point in boot, not git-specific). All three had fully correct content but never reached the final `mv` into place — exactly the incomplete-copy scenario `S04`'s activation manager is designed to detect and safely reject (confirmed: it did).
- **A secondary, unresolved detail**: the `.partial` directories were found nested one level deeper than the code implies (`apps/klipper/klipper.partial` rather than the sibling `apps/klipper.partial` a direct read of `seed_git_app`/`seed_mainsail` and a clean manual reproduction both predict) — cleaned up successfully once the correct nested path was used, but the exact mechanism producing the extra nesting on this real boot was not conclusively identified. Flagged honestly as unresolved; does not appear to be memory-related (it reproduced for the git-free Mainsail case too) and does not affect correctness of `S04`'s own incomplete-copy rejection, but should be revisited before Phase 7 relies on this exact seeding code path again.
- Cleaned up all three stuck `.partial` directories (read-only-adjacent housekeeping, not a state-changing risk to the running system).

**This is a real architectural decision point, not a routine fix** — it affects every future git-based operation this mission's design depends on (Phase 4's seeding, and much more heavily, Phase 7's live `git fetch`/`reset --hard` updates and Moonraker's own dependency installs, which will be far more memory-hungry than an 11.5MB bundle clone). Options requiring a real choice: (a) add `CONFIG_SWAP`(+`CONFIG_SWAP_FILE` or equivalent) to the kernel config and rebuild — a real, targeted kernel change, using the swap file stock already provisions; (b) reduce concurrent memory pressure during git operations (e.g., temporarily stopping non-essential services); (c) some combination. Not decided or implemented yet — reported to the user as a genuine blocker requiring input before Phase 7 proceeds.

## 4. Pacing

Per explicit user direction, proceeding straight through the remaining phases, checkpointing at major risk points (live-device tests, anything that looks destructive) rather than after every phase. Next: **Phase 2** (add git/curl/rsync/openssl/CA-certs/unzip/tar/venv/pip/setuptools/wheel to the Buildroot rootfs and prove real HTTPS against the required GitHub endpoints) — self-contained, no device changes, matches the mission's own phase ordering, unblocks Phase 7's dependency installs and Phase 10's git-based update manager.

## 5. Addendum: config-path/root consistency reconfirmed (mainline print-controls mission, 2026-07-29)

A follow-up report described Mainsail's Config Files page as empty, raising the possibility that the `printer_data` bind-mount architecture described throughout this document (most directly at line 11's `/usr/data/printer_data/gcodes` reference and the `S01persistent-datastore` mount-binding discussion in §3.8) had drifted - Klipper, Moonraker, and Mainsail each resolving to a different directory. Live investigation found the opposite: the bind mount, Klipper's launch argument, Moonraker's `-d`/`-c` arguments, and Moonraker's own `/server/files/roots` API all still agree on exactly the same canonical `/opt/printer_data/config` path this document's own architecture describes, confirmed at the inode level through the real Moonraker file-manager API (not inferred from directory listings alone). No architectural change was needed. New `06-verify.sh` checks now guard this at build time. Full investigation: `docs/NEBULAOS_FRONTEND_PRINT_CONTROLS.md` §7.
